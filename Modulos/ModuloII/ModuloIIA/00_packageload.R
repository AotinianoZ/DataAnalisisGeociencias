# Load libraries for the NADA online course 
loadlibs <- function(x =0){
  require(fitdistrplus)
  require(Kendall)
  require(multcomp)
  require(NADA)
  require(perm)
  require(survminer)
  #   additional packages
  require(car)
  require(bestglm)
  require(rms)
  require(vegan) 
  require(cenGAM)
  require(mgcv)
  require(nlme)
  require(coin)
  require(NADA2)
  require(EnvStats)
  
  # read files
  require(plotly)
  require(readxl)
  require(ggplot2)
  require(dplyr)
  require(tidyverse) # for data analysis
  require(psych)
  require(ggrepel)
  require(gridExtra)
  require(grid)
  require(psych)
  require(ggpubr)

  
}
loadlibs()
cat(" R libraries loaded for Water Analisis And Data Analysis","\n", " written by A. Otiniano & J. Andrade & C.Fernández, Water Analysis.")




